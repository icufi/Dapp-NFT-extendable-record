import React from 'react';

import Accordion from '../shared/components/AccordionUsers';
import ValueProp from '../shared/components/ValueProp';


const Main = () => {
  return (
    <React.Fragment>
      <ValueProp />
      <Accordion />
    </React.Fragment>
  );
};

export default Main;
